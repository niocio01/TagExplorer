<Window x:Class="TagExplorer.Views.IconSelector_V"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TagExplorer.Views"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:viewModels="clr-namespace:TagExplorer.ViewModels"
        Style="{StaticResource MaterialDesignWindow}"
        TextElement.Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
        Background="{DynamicResource MaterialDesign.Brush.Background}"
        mc:Ignorable="d"
        Title="IconSelector_V" Height="450" Width="800"
        x:Name="Root">
    <Grid
        Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock 
            Grid.Row="0"
            Text="Select Icon"
            Style="{StaticResource MaterialDesignHeadline5TextBlock}"
            />
        <Grid
            Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox
                Grid.Column="0"
                Margin="0 5 0 10"
                VerticalAlignment="Center"
                materialDesign:TextFieldAssist.HasLeadingIcon="False"
                materialDesign:HintAssist.Hint="Search"
                Text="{Binding Path=FilterText, ElementName=Root, UpdateSourceTrigger=PropertyChanged}"
                d:Text="Icon Search Text"
                Style="{StaticResource MaterialDesignOutlinedTextBox}" />
            <CheckBox 
                Grid.Column="2"
                Content="Favourites Only"
                IsChecked="{Binding Path=FavouritesOnly, ElementName=Root}"
                Style="{StaticResource MaterialDesignCheckBox}"/>
        </Grid>

        <ScrollViewer 
            Grid.Row="4"
            VerticalScrollBarVisibility="Auto">
            <ItemsControl
                ItemsSource="{Binding Path=FilteredIcons, ElementName=Root}"
                Padding="0 2 0 0">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl></ScrollViewer>

    </Grid>
</Window>
