<UserControl x:Class="TagExplorer.Views.ColorButton_V"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TagExplorer.Views"
             xmlns:viewModels="clr-namespace:TagExplorer.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="30"
             d:DataContext="{d:DesignInstance Type=viewModels:ColorButton_VM, IsDesignTimeCreatable=True}" Loaded="UserControl_Loaded">
    <Grid>
        <Button
            BorderThickness="0"
            Height="20" Width="20"
            Command="{Binding Path=SelectColorCommand}"
            ToolTip="{Binding Path=Color.Name}"
            Padding="0"
            Margin="2"
            Background="Transparent">
            <Border
                BorderThickness="2">
                <Border.Style>
                    <Style TargetType="{x:Type Border}">
                        <Setter Property="BorderBrush"
                                Value="Transparent" />
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver"
                                     Value="True">
                                <Setter Property="BorderBrush"
                                        Value="{DynamicResource MaterialDesign.Brush.Light.Foreground}" />
                            </Trigger>
                            <DataTrigger Binding="{Binding Path=IsSelected}" Value="True">
                                <Setter Property="BorderBrush" Value="White" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Rectangle
                    Height="20" Width="20"
                    Fill="{Binding Path=Color.HexCode}"
                    d:Fill="Red"/>
            </Border>
        </Button>

    </Grid>
</UserControl>
